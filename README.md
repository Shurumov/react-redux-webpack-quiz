# Quiz test work

### Running

```bash
# after cloning the repo:

# install packages
$ npm i

# for dev run
$ npm run dev
# Build availible by default to http://localhost:8020

# for prod build
$ npm run build
# for start server with prod build
$ npm run start
build will availible on http://localhost:3000

```
